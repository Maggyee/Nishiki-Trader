# Binance testnet credentials are loaded only by Phase 3 testnet tooling.
# Keep values empty in git; set real values locally outside the repository.
BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=
