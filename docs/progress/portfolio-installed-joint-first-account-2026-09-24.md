# Installed first ordered four-GET account pass

Date: 2026-09-24. Phase: disposable local fixture only. The
[machine-readable summary](portfolio-installed-joint-first-account-2026-09-24.json)
pins ignored original reports under `data/`. No host installation, venue
request, production key or trading permission was used.

Manifest v19 pins 24 project-owned sources. A new single-use parent accepts
`clock_initial`, `account_connect`, `account_subscribe`, then the first
`account`, `openOrders`, `openOrders`, `account` pass. The root keeps one verified
account WS open across the four signed REST reads. Each native selector uses
its fixed index (3 through 6) and a dedicated UID; each GET has its own
durable attempt, original TLS chunks/receive clocks, kernel grant, revocation
and exact native account or order receipt before parent acceptance. The parent
checks the root route/network/trust and native runtime against its first
original. The four fixed assets, account UID, repeated order identities,
order locks and repeated balances must reconcile. Releasing the subscription
signer after its accepted reply keeps the existing 1,024-descriptor limit.

All three namespace scenarios pass. `joint_reads_success` accepts seven
ordered steps; `joint_reads_orders_changed` prepares six and accepts five;
`joint_reads_balance_drift` prepares seven and accepts six. Each consumed
scope refuses a fresh-process restart and leaves the kernel permit revoked.
Peak sampled descriptors are 863/1,024. Three previous v18 WS, two v17 clock
and two v16 unsubscribe scenarios pass against the v19 source inventory.
The relevant Python tests pass as two separate invocations: 374 shared
sequence/signature/WS tests and 20 snapshot tests. The snapshot module requires
its own invocation because of the existing pytest fixture registration order.

Only **7 of 19 planned operations** have joined this parent. The pass contains
four known fixture assets, not a qualified real all-asset account or an atomic
snapshot. There is no market segment, second four-GET account pass, complete
before/after interval or stream fence. The separate old route and unsubscribe
fixtures cannot be appended to these originals. Next integrate same-run
exchangeInfo/bookTicker route fixation, market/depth and linked time steps,
then the second four-GET pass and account unsubscribe in this same parent.
Provider charge/clock authority, complete shared egress coverage, host rollout,
qualified equity and all real trading remain blocked. Consumed scopes stay
closed.

To repeat with a new disposable path:

```bash
/usr/bin/python3 -I infra/egress-guard/installed_gateway_selftest.py \
  --joint-reads-profile --report data/NEW-JOINT-READS.json
```
