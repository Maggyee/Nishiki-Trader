# Installed ordered joint fixture completion

Date: 2026-09-25. Phase: disposable local fixture only. The
[machine-readable summary](portfolio-installed-joint-complete-acceptance-2026-09-25.json)
pins the ignored original report and its SHA256. No real venue request,
production credential, host installation or trading permission was used.

Manifest `portfolio.installed_gateway_fixture.v27` pins 26 project-owned
sources. The distinct `portfolio.installed_joint_complete.v1` parent preserves
both four-GET fixture account passes, two held WebSockets, four original market
increments, two linked depth snapshots and three native clock receipts. Index
18 appends one fixed `userDataStream.unsubscribe` request for subscription ID
0 on the held account socket. A fresh dedicated-UID native child selects the
same-run route-bound request. Root preserves its exact masked frame, raw WS
response chunks and receive clocks, and requires the exact ID/status/empty
result before acceptance. The market socket remains open; each step has its
own persisted preparation, kernel grant and revocation. Both sockets close
after the terminal acknowledgement or refusal.

Both isolated scenarios pass. Success prepares and accepts **19/19** ordered
steps. A foreign unsubscribe response leaves index 18 pending with 19 prepared
and 18 accepted, without parent acknowledgement or replayable completion.
Both parents refuse fresh-process reuse. The successful sampled descriptor
peak is **945/1,024**, preserving 79 against the unchanged 64-spare
requirement. Forty-eight direct tests and Ruff pass. Earlier source-profile
isolation regressions have not run under the v27 inventory; their historical
reports retain their own hashes and cannot certify v27.
An expanded sandbox regression attempt had 86 passes and 47 setup errors at
`SO_PASSCRED` (`Operation not permitted`); those 47 test bodies did not run.
The earlier automatic approval rejection of the broader elevated suite remains
in force, so this attempt was not rerun with elevated permission.

This closes the fixed 19-operation fixture plan, not a real full-account
interval. Only four known fixture assets reconcile. There is no proven
account-stream fence, full-asset market depth and valuation, provider clock
or usage qualification, real source/gateway authority, enforced shared-egress
coverage, host rollout, qualified equity or live admission. The next entrypoint
is an offline authority and capacity review against actual all-asset inputs,
followed by a separately controlled testnet collection only when its
documented gates are met. Consumed fixture scopes must remain closed.
