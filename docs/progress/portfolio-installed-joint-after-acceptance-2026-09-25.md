# Installed second four-GET account pass

Date: 2026-09-25. Phase: disposable local fixture only. The
[machine-readable summary](portfolio-installed-joint-after-acceptance-2026-09-25.json)
pins the ignored original report and its SHA256. No real venue request,
production credential, host installation or trading permission was used.

Manifest `portfolio.installed_gateway_fixture.v25` pins 26 project-owned
sources; each recorded hash matches this acceptance checkout. A distinct
`portfolio.installed_joint_account_after.v1` parent preserves the v24
clock, account/market WebSockets, first four-GET pass, fixed routes,
four buffered market increments, two linked depth snapshots and second time
GET. Indices 13-16 append signed account, openOrders, openOrders and account
GETs while both WebSockets stay open. Each dedicated-UID selector binds the
same-run route hash; the root persists the fixed request, original HTTPS
bytes/clocks and native balance/order receipt under its own revoked attempt.
Replay compares the four known fixture assets, account UID, open-order sets
and remaining locks within and between the two passes, and bounds the final
account receipt to the first market event on the same UTC day.

Three isolated scenarios pass. Success prepares and accepts **17/19** steps.
A changed second-pass order is received natively but index 15 remains pending:
16 prepared, 15 accepted. A changed final balance similarly leaves index 16
pending: 17 prepared, 16 accepted. Both parents refuse fresh-process reuse.
Maximum sampled descriptors are **953/1,024**, leaving only 71; the next
two operations must preserve the 64-spare requirement. The 96 focused Python
tests, Ruff lint/format and original source-hash checks pass.

The full old-profile v25 isolation regression set has **not** run: the local
sandbox denied `SO_PASSCRED` for the broad pytest suite, and automatic approval
review rejected its elevated rerun as broader than the authorized disposable
fixture. The v24 reports retain their own hashes and historical status; they
cannot certify this v25 source inventory.

Only the bounded four-asset fixture before/after reads are reconciled. The
final clock GET and account WebSocket unsubscribe remain indices 17-18 in a
fresh parent; no complete real all-asset account interval, provider clock
qualification, stream fence, real gateway authority/coverage, qualified equity
or live admission follows. Consumed scopes stay closed.
