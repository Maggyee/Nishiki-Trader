# Installed final joint clock

Date: 2026-09-25. Phase: disposable local fixture only. The
[machine-readable summary](portfolio-installed-joint-final-clock-acceptance-2026-09-25.json)
pins the ignored original report and its SHA256. No real venue request,
production credential, host installation or trading permission was used.

Manifest `portfolio.installed_gateway_fixture.v26` pins 26 project-owned
sources. A distinct `portfolio.installed_joint_final_clock.v1` parent preserves
the two four-GET account passes, two held WebSockets, bounded original market
increments, two linked depth snapshots and the earlier clocks. Index 17 adds
one route-bound native `/api/v3/time` GET under its own prepared, revoked
attempt. Original receive clocks place its response after the second account
pass and within 60 seconds of the first market event on one UTC day. The
fixture does not qualify a provider clock or a real all-asset account interval.

Two isolated scenarios pass. Success prepares and accepts **18/19** ordered
steps. A server clock displaced by six seconds consumes index 17 without
native or parent acknowledgement: 18 prepared, 17 accepted. Both parents
refuse fresh-process reuse. The new parent releases only previously accepted
step README descriptors, checking their inode and bytes on each later
verification; originals and directory descriptors remain held. Successful
sampled descriptors peak at **945/1,024**, leaving 79 against the unchanged
64-spare requirement. The 21 direct tests and Ruff pass. The historical v25
reports remain pinned to their original source inventory; broad old-profile
v26 isolation regressions have not run after the earlier approval rejection.

The final account WS unsubscribe is still index 18 under a new, unconsumed
parent. Four fixture assets reconcile; no complete real account coverage,
stream fence, provider usage, real gateway authority or live admission follows.
